Config = {
    Teleport = {
        Enabled = false,
        Target = "Spawn"
    },

    Farming = {
        Enabled = false,
        Delay = 1.0
    },

    Fishing = {
        Enabled = false,
        Speed = 0.5
    }
}

return Config
